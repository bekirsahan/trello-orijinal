# SQLite için varsayılan (Hiçbir kurulum gerektirmeden direkt çalışır):
DATABASE_URL="file:./dev.db"

# PostgreSQL için (Lokalinizde PostgreSQL çalışıyorsa schema.prisma'daki provider'ı "postgresql" yapıp burayı açın):
# DATABASE_URL="postgresql://postgres:password@localhost:5432/trello_db?schema=public"

PORT=5000
JWT_SECRET="trello_super_secret_key_12345"
NODE_ENV="development"
